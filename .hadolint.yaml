# hadolint configuration for the add-on Dockerfiles.
ignored:
  # DL3018: "Pin versions in apk add". The runtime Node.js/JRE packages come
  # from the Home Assistant base image's Alpine repository. Pinning exact apk
  # versions here would break builds whenever that repo snapshot rolls
  # forward, so we pin the base image digest in build.yaml instead and let
  # apk versions float within that frozen base.
  - DL3018
